/* 适老化卡片组件样式 */
.elderly-card {
  background-color: #FFFFFF;
  border-radius: 12rpx;
  box-shadow: 0 2rpx 12rpx 0 rgba(0, 0, 0, 0.1);
  margin: 20rpx 0;
  box-sizing: border-box;
  overflow: hidden;
}